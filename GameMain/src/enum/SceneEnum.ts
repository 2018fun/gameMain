/**
 * 
 */

module SceneEnum {
    let auto = 0;
    export const TITLE_SCENE = auto++;
    export const GAME_SCENE = auto++;
    export const CHAR_SCENE = auto++;
    export const CITY_SCENE = auto++;
    export const ITEM_SCENE = auto++;
}